import 'package:flutter/cupertino.dart';

const kHorizontalListMargin = EdgeInsets.only(left: 20);

const kPosterBorderRadius = BorderRadius.all(Radius.circular(20));

const kMainColumnBottomPadding = EdgeInsets.only(bottom: 20);

const kWatchButtonBorderRadius = BorderRadius.all(Radius.circular(100));

const kDetailContentPadding = EdgeInsets.only(left: 20, right: 20, bottom: 20);
